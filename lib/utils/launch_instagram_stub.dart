// Este archivo es para Android/iOS/etc. y no hace nada
void openInstagramWeb(String url) {
  // No hace nada, o podés poner un print para debug
  print("Web launch ignored en plataforma no compatible.");
}
